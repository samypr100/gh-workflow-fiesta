[project]
name = "bench-contract"
version = "0.1.0"
description = "Shared data contract for the concurrency benchmark explorer"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["pydantic>=2.10"]

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
